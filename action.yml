name: "Azure Resource Group Deployment"
description: "todo..."
inputs:
  ressourceGroup:
    description: "todo..rg"
    required: true
  templateFile:
    description: "todo..template"
    required: true
outputs:
  time: # id of output
    description: "The time we greeted you"
runs:
  using: "node12"
  main: "index.js"
